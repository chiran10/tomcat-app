apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: enterprise-app
spec:
  selector:
    app: postgres      # must match the label on your PostgreSQL pod
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
