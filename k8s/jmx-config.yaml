apiVersion: v1
kind: ConfigMap
metadata:
  name: jmx-config
  namespace: enterprise-app
data:
  config.yaml: |
    startDelaySeconds: 0
    lowercaseOutputName: true
    lowercaseOutputLabelNames: true
    rules:
      - pattern: ".*"
